# v2.12 终稿格式确认版修订说明

本版基于 `paper_draft_v2.11_final.md` 生成，只处理最终复审中提出的格式与可读性问题，不改模型、不改参数、不改任何核心结果数值。

## 已处理事项

1. **页码位置修正**
   - 将摘要页起始页码改为页脚中部居中显示；
   - 删除正文页顶部页眉和右上角页码；
   - 保持封面页不编号。

2. **页面尺寸修正**
   - 输出 PDF 页面尺寸设为 A4；
   - 页边距保持 2.5 cm。

3. **一级标题显示确认**
   - “一、问题重述”“二、问题分析”等一级标题保持居中显示；
   - 取消页眉后，第八章页眉 TARGET90 大写问题随之消失。

4. **表 8.3 中 410 表述修正**
   - 将“这里的 410 指……”改为“算法首先生成 410 个初始 day--spot--depart 候选三元组”，避免被误读为乘法表达式或不明数字。

5. **表 9.1 low_commute 数值列修正**
   - 将 R / P_fail 列中的“参考 robust”改为 `≈0.0011* / ≈0.9989*`；
   - 表注说明该值仅为参考 robust 的量级结果，未作为 Q3 主可靠度对比对象单独复核。

6. **表 7.1 来源字段缩短**
   - 将 `reliability_report.json.assumptions.N_simulations` 缩短为 `assumptions.N_simulations`；
   - 将 `reliability_report.json.assumptions.seed` 缩短为 `assumptions.seed`，避免列宽误判为截断。

7. **表 8.5 表头可读性优化**
   - 将 `back_late 次数` 改为“返店超时数”；
   - 将 `insufficient_visit_time 次数` 改为“短游次数”；
   - 将“主要失败原因”改为“主要原因”，避免表头挤压。

## 渲染检查

已重新渲染检查以下页面：

- 第 1 页：封面无页码，邮箱完整显示；
- 第 2 页：摘要页页码为页脚中部 `1`；
- 第 3 页：一级标题居中，无页眉；
- 表 7.1 所在页：`N_simulations` 字段不再被误判为截断；
- 表 8.5 所在页：表头不再重叠；
- 附录 A/C/D：长路径仍可读，无明显越界。

## 提交建议

若不再新增模型或图表，建议以 `五一五日自驾景点优选与行程规划_最终提交版_v2.12.pdf` 作为最终论文 PDF。
