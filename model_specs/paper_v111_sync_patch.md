# paper_v111_sync_patch.md

> 中国海洋大学数学建模校赛 C 题 —— 论文同步补丁（基于 v1.1.1 边界补丁）
> 配套文件：`paper_draft_placeholder.md`（待填版） + `model_spec_v1.1.1_boundary_patch.md`
> 性质：**仅列出论文需要按 v1.1.1 修改的位置**，不重写论文、不填数值、不引入新指标
> 修订原则：定位 → 操作（增 / 改 / 删）→ 依据章节，便于晚上填论文时逐条对照

---

## 1. 摘要修订定位

逐句对照 `paper_draft_placeholder.md` §摘要（第 11–19 行），按下表处理：

| 位置 | 旧表述 | 操作 | 依据 |
|---|---|---|---|
| 第 1 段（行 11） | "四 / 五套偏好备选方案" | **改为**"5 套偏好备选方案" | 补丁 §4.5 |
| 第 1 段（行 11） | "活动时间 7:00–21:00 等硬约束" | **保留** 7:00–21:00 描述，但**删除**"每日统一 8:30 出发"的隐含表述（若有）| 补丁 §1 |
| 问题二段（行 15） | "引入三类决策变量 …，固定每日 8:30 出发，午餐 1.0 h 等效插入首个景点游览结束后" | **改写**："采用 day-specific 出发时刻（Day 1 11:30、Day 2–4 8:30），Day 5 最迟 16:30 回酒店，午餐 1.0 h 等效插入首个景点游览结束后" | 补丁 §1.2 |
| 问题二段（行 15） | "活动窗 $T_k^{\text{all}}\le 14$ 与返程时刻约束" | **改写**："day-specific 景区耗时上限 $T_k^{\text{scenic,max}} \in \{9.5, 12.5, 12.5, 12.5, 8.0\}$ 与对应返程时刻 $\tau_k^{\text{back,max}}$" | 补丁 §1.2 / §1.3 |
| 问题三段（行 17） | "失败事件为 $T_k^{\text{scenic}} > 12.5 \vee \tau_k^{\text{back}} > 21 \vee \exists i:\,\tilde t_i < l_i^{\min}$ 的 OR 关系" | **改为**"失败事件为 $\tau_k^{\text{back}} > \tau_k^{\text{back,max}} \vee \exists i:\,\tilde t^{(s)}_i < l_i^{\min}$ 的两项 OR 关系" | 补丁 §3 |
| 问题三段（行 17） | "为回应 $R \ge 90\%$ 的目标，本文在不改变 8:30 出发与景区内口径的前提下引入时间缓冲指标 $Z_4 = \min_k(12.5 - T_k^{\text{scenic}})$ 与稳健优先型方案" | **改写**："在 $R \ge 90\%$ 的**假设性达标要求**下识别薄弱点；定义时间缓冲指标 $Z_4 = \min_k(T_k^{\text{scenic,max}} - T_k^{\text{scenic}})$ 与稳健优先型方案，作为 5 套并列偏好之一" | 补丁 §1.5 / §4.5 |
| 主要结论 (3)（行 19） | "稳健优先型方案在保留 8:30 出发的前提下" | **改为**"稳健优先型方案在 day-specific 边界下" | 补丁 §1 |
| 主要结论 (4)（行 19） | "配合外部分流与内部牺牲的两类达标路径" | **删除**"外部分流（节假日交通管控）"的提法；改为"通过敏感性分析识别 $R \ge 90\%$ 的扰动阈值区，并结合方案收缩讨论达标路径" | 补丁 §7 表 §6.10 修订 |

**摘要必须删除的措辞**：
- "8:30 出发"（任何位置）
- "$T_k^{\text{all}} \le 14$" / "活动窗 14"
- "12.5"（作为 day-specific 上限的旧值；公式中可保留作为 day 2–4 实例）
- "v1.1 新增" / "补丁新增" / "四 + 1" / "四 / 五套"
- "外部分流：节假日交通管控"

---

## 2. 模型假设修订定位（§2）

### 2.1 必须修改的旧假设

| 假设号 | 旧内容（节选） | 操作 | 依据 |
|---|---|---|---|
| 假设 1（行 71） | "以每日 8:30 离开酒店出发" | **改为**"按 day-specific 出发时刻 $\tau^{\text{dep}}_k$ 离开酒店" | 补丁 §1 |
| 假设 2（行 73） | 整条"每天从酒店出发的时刻统一为 $\tau^{\text{dep}} = 8.5$" | **整条改写**：陈述 day-specific $\tau^{\text{dep}}_k$ 与 $\tau^{\text{back,max}}_k$，并指出"Day 1 与 Day 5 为半日景区活动" | 补丁 §1.2 / §1.6 |
| 假设 4（行 77） | "家↔酒店单程车程 4.0 h、酒店入住 0.5 h（5 月 1 日）、酒店退房 0.5 h（5 月 5 日）…合计 17.0 h" | **保留主要内容**，但**新增一句**："上述固定边界耗时已用于推导 Day 1 的 $\tau^{\text{dep}}_1 = 11.5$ 与 Day 5 的 $\tau^{\text{back,max}}_5 = 16.5$（详见假设 16）" | 补丁 §1.2 |
| 假设 5（行 79–81） | "$T_k^{\text{all}}\le 14 \Leftrightarrow T_k^{\text{scenic}}\le 12.5$" | **整条删除**（在 day-specific 下不再适用）；或保留为"$T_k^{\text{scenic}}\le T_k^{\text{scenic,max}}$（day-specific，详见假设 16）" | 补丁 §1.3 |
| 假设 13（行 103–105） | "$T_k^{\text{scenic}} > 12.5 \;\vee\; \tau_k^{\text{back}} > 21 \;\vee\; \exists i:\,\tilde t_i < l_i^{\min}$" | **改为两项 OR**："$\tau_k^{\text{back}} > \tau_k^{\text{back,max}} \;\vee\; \exists i \in \text{visit}_k:\,\tilde t_i < l_i^{\min}$" | 补丁 §3.1 |
| 假设 15（行 109） | "问题二采用 4 套偏好…问题三在 v1.1 扩展中追加 robust 第 5 套权重" | **改写**："问题二输出 5 套偏好（recommend、preference、low_commute、balanced、robust）的 top-1 方案；其中 recommend 为问题二主基准、robust 为问题三稳健对比" | 补丁 §4.4 |

### 2.2 必须新增的假设

| 编号 | 新增内容 | 依据 |
|---|---|---|
| **假设 16（新增）** | 5 日行程的 day-specific 边界处理。Day 1：家 7:00 出发，4.0 h 车程 + 0.5 h 入住后 11:30 开始景区活动；Day 2–4：8:30 出发；Day 5：最迟 16:30 回酒店，预留 0.5 h 退房 + 4.0 h 返家，21:00 前到家。$\tau^{\text{dep}}_k = [11.5, 8.5, 8.5, 8.5, 8.5]$，$\tau^{\text{back,max}}_k = [21.0, 21.0, 21.0, 21.0, 16.5]$，$T^{\text{scenic,max}}_k = [9.5, 12.5, 12.5, 12.5, 8.0]$ | 补丁 §1.6 |
| **假设 17（新增）** | 早到等候为合法状态。若 $\tau_{k,i}^{\text{arr}} < o_i^{\text{op}}$，引入实际游览开始时刻 $\tau_{k,i}^{\text{visit\_st}} = \max(\tau_{k,i}^{\text{arr}}, o_i^{\text{op}})$；等候时间占用日内景区耗时 $T_k^{\text{scenic}}$，但不计入游览时长 $t_i$、不计入排队 $Q_i$；排队峰段判定基于 $\tau_{k,i}^{\text{visit\_st}}$ | 补丁 §2 |

### 2.3 必须新增的"建模假设说明"

在假设 16 末尾**显式标注**两条"非题目给定"的建模假设：

> (i) Day 1 家 7:00 出发为建模假设（题目仅给"起床+早餐+整装 1.5 h"的耗时标准，未指定家出发时刻）；  
> (ii) Day 5 以 21:00 到家作为"不安排夜间长途行车"的临界解读（题目仅给"每日活动 7:00–21:00"上限）。

---

## 3. 符号说明修订定位（§3）

### 3.1 §3.2 问题二符号表（行 137–157）必须修改的行

| 旧符号行 | 操作 |
|---|---|
| `$T_k^{\text{scenic}}$ | 景区内总耗时，$=\tau_k^{\text{back}} - 8.5$` | **改为** `$=\tau_k^{\text{back}} - \tau^{\text{dep}}_k$` |
| `$T_k^{\text{all}}$ | 全日总耗时，$=1.5 + T_k^{\text{scenic}}$` | **删除整行**（不再参与校验；如保留仅作为诊断量需加注） |
| `$Z_4$（v1.1 新增） | 时间缓冲指标` | **删除"（v1.1 新增）"标签**，改为 `$Z_4$ | 时间缓冲指标` |
| `$F,\,F_{\text{robust}}$ | 综合评分（v1.0 / v1.1）` | **改为** `$F$ | 综合评分（5 套偏好共用）` |
| `$B_k$ | 第 $k$ 天时间缓冲` | **改为** `$B_k = T^{\text{scenic,max}}_k - T_k^{\text{scenic}}$ | 第 $k$ 天时间缓冲（day-specific）` |

### 3.2 §3.2 必须新增的符号行

| 新增符号 | 含义 |
|---|---|
| $\tau^{\text{dep}}_k$ | 第 $k$ 天从酒店出发时刻（day-specific） |
| $\tau^{\text{back,max}}_k$ | 第 $k$ 天最迟返回酒店时刻（day-specific） |
| $T^{\text{scenic,max}}_k$ | 第 $k$ 天景区耗时上限，$= \tau^{\text{back,max}}_k - \tau^{\text{dep}}_k$ |
| $\tau_{k,i}^{\text{visit\_st}}$ | 第 $k$ 天景点 $i$ 的实际游览开始时刻，$= \max(\tau_{k,i}^{\text{arr}}, o_i^{\text{op}})$ |

### 3.3 §3.3 问题三符号表（行 161–174）必须新增

| 新增符号 | 含义 |
|---|---|
| $\tau_{k,i}^{\text{visit\_st},(s)}$ | 第 $s$ 次模拟下景点 $i$ 的实际游览开始时刻 |

（其余问题三符号沿用 §3.2 的 day-specific 边界，不另增。）

### 3.4 §3.1 问题一符号表（行 117–135）

**不修改**。补丁 §6.4 已明确：问题一保留 $\tau^{\text{dep}} = 8.5$ 作为评分专用基准，与问题二 day-specific 解耦。需要在 §4.3（公式 4 附近）**新增一句脚注**：

> "本式中的 $\tau^{\text{dep}} = 8.5$ 为问题一专有的评分基准，不与问题二 / 问题三的 day-specific 出发时刻混淆。详见假设 16。"

---

## 4. 问题二同步定位（§5）

### 4.1 §5.2 约束体系修订（行 298–334）

| 约束号 | 旧式 | 操作 |
|---|---|---|
| (C7) 公式 (20) | $T_k^{\text{all}} \le 14 \Leftrightarrow T_k^{\text{scenic}} \le 12.5$ | **改为** $\tau_k^{\text{back}} \le \tau^{\text{back,max}}_k$，等价 $T_k^{\text{scenic}} \le T^{\text{scenic,max}}_k$，标号沿用 (C7') |
| (C8) 公式 (21) | $o_i^{\text{op}} \le \tau_{k,i}^{\text{arr}},\;\tau_{k,i}^{\text{lv}} \le o_i^{\text{cl}}$ | **改为** $\tau_{k,i}^{\text{lv}} \le o_i^{\text{cl}}$（**删除**左半部分，因引入 visit_st 后早到合法），标号沿用 (C8') |
| (C9) 公式 (22) | $\tau_k^{\text{back}} \le 21.0$ | **整条删除**（与 (C7') 合并） |

公式编号 (20)–(22) 需相应调整，后续公式编号需顺移或保留空号（建议保留 (22) 为占位空号注明"已合并入 (20)"，避免后续公式编号大规模重排）。

### 4.2 §5.3 时间轴修订（行 336–354）

| 公式号 | 旧式 | 操作 |
|---|---|---|
| (23) | $\tau_{k,i}^{\text{arr}} = 8.5 + D_{0,i},\;\tau_{k,i}^{\text{lv}} = \tau_{k,i}^{\text{arr}} + t_i$ | **改为** $\tau_{k,i}^{\text{arr}} = \tau^{\text{dep}}_k + D_{0,i}$；**新增**一行 $\tau_{k,i}^{\text{visit\_st}} = \max(\tau_{k,i}^{\text{arr}}, o_i^{\text{op}})$；**改** $\tau_{k,i}^{\text{lv}} = \tau_{k,i}^{\text{visit\_st}} + t_i$ |
| (25) | $\tau_{k,j}^{\text{arr}} = \tau_k^{\text{lun\_ed}} + D_{i,j},\;\tau_{k,j}^{\text{lv}} = \tau_{k,j}^{\text{arr}} + t_j$ | **同 (23) 处理**：新增 $\tau_{k,j}^{\text{visit\_st}}$ 一行；$\tau_{k,j}^{\text{lv}} = \tau_{k,j}^{\text{visit\_st}} + t_j$ |
| (24)、(26)、(27) | 不变 | 保留 |

**新增一段说明文字**（紧接公式 (23) 修订后）：

> "$\tau_{k,i}^{\text{visit\_st}}$ 表示考虑景点开放时间后的实际游览开始时刻。若 $\tau_{k,i}^{\text{arr}} < o_i^{\text{op}}$，差值即为早到等候时间，占用日内景区耗时但不计入游览时长 $t_i$。"

### 4.3 §5.4 $Z_4$ 同步（行 397–409）

| 公式号 | 旧式 | 操作 |
|---|---|---|
| (33) | $B_k = 12.5 - T_k^{\text{scenic}},\;Z_4 = \min_k B_k$ | **改为** $B_k = T^{\text{scenic,max}}_k - T_k^{\text{scenic}}$（其余 $Z_4 = \min_k B_k$ 不变） |
| (34) | $\tilde Z_4$ 归一化 | 不变（基准随 $B_k$ 自动同步） |
| (35) | $F_{\text{robust}}$ 综合评分 | **改名**为 $F$（删除"robust"下标），并删除"$F_{\text{robust}}$"这个符号 |

**新增一段说明文字**（紧接 (33) 后）：

> "由于 $T^{\text{scenic,max}}_k$ 对 Day 1 (9.5)、Day 2–4 (12.5)、Day 5 (8.0) 取值不同，$B_k$ 反映各天**绝对小时数余量**，可直接与堵车 / 排队延时分布的支撑集（小时）比对，物理意义在 day-specific 下仍然成立。"

### 4.4 §5.6 / §5.7 偏好方案合并

**操作**：将 §5.6（v1.0 四套）与 §5.7（v1.1 新增 robust）**合并为一个 §5.6**，标题改为 **"5 套偏好方案"**。

| 旧位置 | 操作 |
|---|---|
| §5.6 标题"v1.0 四套偏好方案" | **改为**"5 套偏好方案" |
| §5.6 四行权重表 | **扩为五行**，加入 robust = $(0.4, 0.2, 0.2, 0.2)$ |
| §5.6 末句"主基准行程取 `recommend` 下的 top-1" | **保留** |
| §5.7 整节"v1.1 新增：…与 robust 稳健优先型方案" | **解构**：$Z_4$ 与 $B_k$ 的定义并入 §5.4 末尾（与 $Z_3$ 并列陈述"两层均衡含义"）；权重表并入新 §5.6；**删除**"v1.1 新增"、"向后兼容"等措辞 |
| §5.7 末"原 v1.0 四套偏好均可视为 $\lambda_4 = 0$ 的特例" | **删除** |

**新增一段说明文字**（新 §5.6 末尾）：

> "5 套偏好中仅 robust 显式赋予 $\lambda_4 = 0.2$，其余 4 套 $\lambda_4 = 0$。这反映 5 套偏好的语义差异：前 4 套侧重体验、通勤、均衡等不同维度，robust 侧重抵御随机扰动。$Z_4$ 与 $Z_3$ 数学不冗余（见 §5.4 末尾两层均衡含义讨论）。"

### 4.5 §5.8 求解流程修订（行 419–430）

| 旧步骤 | 操作 |
|---|---|
| 第 3 步"活动窗 $T_k^{\text{all}} \le 14$（C7）与 $\tau_k^{\text{back}} \le 21$（C9）" | **改为**"day-specific 上限 $\tau_k^{\text{back}} \le \tau^{\text{back,max}}_k$（C7'）" |
| 第 3 步 | **新增**一行："对每天 $k$ 独立调用单日方案构建函数（因 day-specific 上限不同，单日方案池不能跨天复用）" |

### 4.6 §5.9 结果展示修订（行 432–461）

| 位置 | 旧文字 | 操作 |
|---|---|---|
| 行 443 | "$T_k^{\text{all}} = T_k^{\text{scenic}} + 1.5$ 均不超过 14 h" | **改为**"各日 $T_k^{\text{scenic}}$ 均不超过 day-specific 上限 $T^{\text{scenic,max}}_k$" |
| 行 443 | "返程时刻均 $\le 21:00$" | **改为**"返程时刻均 $\le \tau^{\text{back,max}}_k$（Day 1–4 为 21:00，Day 5 为 16:30）" |
| 行 461 | "v1.1 新增的 `robust` 稳健优先型方案及其与 …的对比将在 §6.8 与 §6.11 中合并展示" | **改为**"`robust` 方案与 `recommend`、`balanced` 的 Monte Carlo 对比在 §6.11 中展示" |

---

## 5. 问题三同步定位（§6）

### 5.1 §6.3 排队采样修订（行 482–485）

| 公式号 | 旧式 | 操作 |
|---|---|---|
| (39) | "$9 \le \tau_{k,i}^{\text{arr},(s)} < 12$" | **改为**"$9 \le \tau_{k,i}^{\text{visit\_st},(s)} < 12$"（排队峰段判定基于 visit_st） |

### 5.2 §6.4 受扰动时间轴修订（行 487–507）

| 公式号 | 旧式 | 操作 |
|---|---|---|
| (40) | $\tau_{k,i}^{\text{arr},(s)} = 8.5 + D_{0,i} + W^{(s)}_{k,1}$ | **改为** $= \tau^{\text{dep}}_k + D_{0,i} + W^{(s)}_{k,1}$ |
| (40) 后 | — | **新增公式** $\tau_{k,i}^{\text{visit\_st},(s)} = \max(\tau_{k,i}^{\text{arr},(s)},\;o_i^{\text{op}})$（建议编号 40b 或重排） |
| (41) | $\tilde t^{(s)}_{k,i} = \max(0,\min(l_i^{\text{comf}},\;o_i^{\text{cl}} - \tau_{k,i}^{\text{arr},(s)} - Q^{(s)}_{k,i}))$ | **改为** $\tilde t^{(s)}_{k,i} = \max(0,\min(l_i^{\text{comf}},\;o_i^{\text{cl}} - \tau_{k,i}^{\text{visit\_st},(s)} - Q^{(s)}_{k,i}))$（用 visit_st 替换 arr） |
| (42) | $\tau_{k,i}^{\text{lv},(s)} = \tau_{k,i}^{\text{arr},(s)} + Q^{(s)}_{k,i} + \tilde t^{(s)}_{k,i}$ | **改为** $\tau_{k,i}^{\text{lv},(s)} = \tau_{k,i}^{\text{visit\_st},(s)} + Q^{(s)}_{k,i} + \tilde t^{(s)}_{k,i}$ |
| (44) | $\tau_{k,j}^{\text{arr},(s)} = \tau_k^{\text{lun\_ed},(s)} + D_{i,j} + W^{(s)}_{k,2}$ | 保留；**新增**一行 $\tau_{k,j}^{\text{visit\_st},(s)} = \max(\tau_{k,j}^{\text{arr},(s)},\;o_j^{\text{op}})$ 用于第二景点 |
| (46) | $T_k^{\text{scenic},(s)} = \tau_k^{\text{back},(s)} - 8.5$ | **改为** $= \tau_k^{\text{back},(s)} - \tau^{\text{dep}}_k$ |

### 5.3 §6.5 失败事件修订（行 511–515）

| 公式号 | 旧式 | 操作 |
|---|---|---|
| (47) | $F_k^{(s)} = \mathbb{1}\{T_k^{\text{scenic},(s)} > 12.5 \vee \tau_k^{\text{back},(s)} > 21 \vee \exists i: \tilde t^{(s)}_{k,i} < l_i^{\min}\}$ | **改为两项 OR**：$F_k^{(s)} = \mathbb{1}\{\tau_k^{\text{back},(s)} > \tau^{\text{back,max}}_k \vee \exists i \in \text{visit}_k: \tilde t^{(s)}_{k,i} < l_i^{\min}\}$ |
| (48) | 整次失败 OR 关系 | 不变 |
| (49)、(50) | $\hat R, \hat P_{\text{fail}}$ 与 CI | 不变 |

**新增说明文字**（紧接 (47) 后）：

> "原 v1.0 中的 OR 关系含三项（$T_k^{\text{scenic}}$、$\tau_k^{\text{back}}$、$\tilde t_i$），由 $T_k^{\text{scenic}} = \tau_k^{\text{back}} - \tau^{\text{dep}}_k$ 与固定 $\tau^{\text{dep}}_k$ 可知前两项数学等价，本文采用两项 OR 形式以消除冗余。"

### 5.4 §6.8 三方案对比修订（行 550–558）

| 位置 | 旧文字 | 操作 |
|---|---|---|
| 标题 | "三方案 Monte Carlo 对比（v1.1 补丁）" | **删除**"（v1.1 补丁）" |
| 第一句 | "在不改变每日 8:30 出发、景区内行程口径与可靠度定义的前提下" | **改为**"在 day-specific 边界、景区内行程口径与可靠度定义下" |
| (iii) | "代表 v1.1 显式纳入 $Z_4$ 缓冲约束的方案" | **改为**"代表显式纳入 $Z_4$ 缓冲约束的方案" |

### 5.5 §6.10 R < 90% 解释修订（行 574–594）

| 位置 | 旧文字 | 操作 |
|---|---|---|
| §6.10 标题 | "关于 $R < 90\%$ 的结构性解释" | **建议改名**为"$R \ge 90\%$ 假设性达标要求下的结构性分析"（与 §1.3 / 摘要措辞一致） |
| (a) | "可靠度低于 90% 反映 … 是题目所要求的'结构性薄弱点'" | 主体保留；**删除**"未达标"等承诺式措辞 |
| (c)(i) | "**外部分流**：通过节假日交通管控将 $p^{\text{pk}}$ 降至 0.5 以下" | **整条改写**："**扰动假设减弱**：当扰动概率落入敏感性扫描的低风险区（如 $p^{\text{pk}} \le 0.5$ 且 $p^{\text{off}} \le 0.2$），$R$ 可达到 90% 以上。此为对扰动假设的稳健性验证，而非对题目条件的修改。" |
| (c)(ii) | "**内部牺牲**：进一步减少景点数 …" | **保留**，措辞改为"**方案进一步收缩**：$\vert S\vert = 5$ 且尽量安排为单景点日，可提升 $Z_4$ 与 $R$" |

**严格禁止**保留"节假日交通管控"作为达标路径（这属于修改题目条件）。

### 5.6 §6.11 求解结果与分析修订（行 596–644）

| 位置 | 操作 |
|---|---|
| 三方案对比表（行 621–626） | 表格结构保留；表头与字段不变。**注**：表中 $Z_4$ 数值需按 day-specific 上限重算（见补丁 §1.5）|
| (c)（行 631） | "若否，则最优方案为…此时按 §6.10 三层结构对该结果作结构性解释" | **保留**，但确认与新 §6.10 措辞一致（删除"达标 / 未达标"二元表述） |
| 行 644 后 | **新增一段**："Day 1 与 Day 5 因活动窗较窄（$T^{\text{scenic,max}} = 9.5$ / $8.0$），$P_{\text{fail},k}$ 可能本就高于 Day 2–4，属窄窗结构性特征，与扰动诱发的薄弱点应分开评论。" |

---

## 6. 结果分析中必须作废的旧结果

以下数值因边界与早到逻辑变更，**全部失效**，需重跑后填入。**严禁直接沿用旧版数值**。

### 6.1 §4.7 问题一结果（行 274–280）

| 项 | 是否作废 |
|---|---|
| 高 / 低优先级景点 ID（F1-01, F1-02） | **可保留**（问题一沿用 $\tau^{\text{dep}} = 8.5$ 评分基准，与边界修订无关） |
| 联动对总数 N1（F1-06） | **可保留**（与边界无关） |
| 同日可行联动对数 N2（F1-07） | **必须重算**（联动可行性检验使用了 $T_k^{\text{all}} \le 14$，需改为 day-specific；详见 §4.5 公式 (13) 附近文字） |
| $C_i$ 主导子项（F1-09） | **可保留** |

### 6.2 §5.9 问题二结果（行 432–461）

**全部数值作废**，必须重跑：

- F2-01 N_feasible（边界变更后可行解集合变化）
- F2-02 ~ F2-09 基准行程的 selected、Z1/Z2/Z3、$\tilde Z_p$、$F$、5 个 $T_k^{\text{scenic}}$
- F2-10 ~ F2-17 五套偏好对比与文本判断
- 表 6（基准行程时间轴）、表 7（方案对比）、图 2

### 6.3 §6.11 问题三结果（行 596–644）

**全部数值作废**，必须重跑：

- F3-01 ~ F3-08 主结果 R、CI、$P_{\text{fail},k}$、$\phi_i$、Shapley
- F3-09 ~ F3-10 敏感性阈值
- F3-11 ~ F3-16 三方案对比
- F3-17 ~ F3-20 薄弱点诊断
- 表 8、表 9、表 10；图 3、图 4、图 5、图 6

### 6.4 摘要数值

摘要中所有【待填】数值均与正文 §4.7 / §5.9 / §6.11 同源，正文重填后**回填**即可。

---

## 7. 待填项清单（附录 D）同步定位

### 7.1 §D.2 问题二（行 813–831）必须新增的字段

在原 17 行 F2-01 ~ F2-17 后**追加**：

| 编号 | 待填内容 | 数据来源 |
|---|---|---|
| F2-18 | 5 天的 $\tau^{\text{dep}}_k$ 数组 | `baseline_plan.json` 字段 `boundary_v111.tau_dep_per_day` |
| F2-19 | 5 天的 $\tau^{\text{back,max}}_k$ 数组 | `baseline_plan.json` 字段 `boundary_v111.tau_back_max_per_day` |
| F2-20 | 5 天的 $T^{\text{scenic,max}}_k$ 数组 | `baseline_plan.json` 字段 `boundary_v111.T_scenic_max_per_day` |
| F2-21 | 基准行程 5 天 buffer $B_k$ | `baseline_plan.baseline_plan.days[].buffer` |
| F2-22 | 基准行程 5 天 visit_st 序列 | `baseline_plan.baseline_plan.days[].visit_st` |
| F2-23 | 基准行程 $Z_4$ | `baseline_plan.baseline_plan.Z4` |

### 7.2 §D.3 问题三（行 835–856）必须新增的字段

在原 20 行 F3-01 ~ F3-20 后**追加**：

| 编号 | 待填内容 | 数据来源 |
|---|---|---|
| F3-21 | boundary_v111 是否生效 | `reliability_report.assumptions.boundary_v111_applied`（应为 true） |
| F3-22 | 早到等候处理方式 | `reliability_report.assumptions.early_arrival_handling`（应为 "wait_until_open"） |
| F3-23 | 三方案 $Z_4$（基于 day-specific 上限） | `reliability_report.compared_plans[].Z4` |
| F3-24 | Day 1 / Day 5 窄窗对 $P_{\text{fail},k}$ 的结构性影响（文本分析） | 文本判断 |

### 7.3 §D.7 最终自检清单（行 893–904）必须新增的检查项

在原清单后**追加**：

- [ ] 摘要、§2、§3、§5、§6 中已无 "8:30" 单值出发表述（除问题一 §4.3 评分基准的脚注）
- [ ] 摘要、§2 假设 5、§5.2 (C7)、§5.9 中已无 "$T_k^{\text{all}} \le 14$"
- [ ] §2 假设 13、§5.2 (C9)、§6.5 公式 (47) 中已无 "$T_k^{\text{scenic}} > 12.5$ 或 $\tau_k^{\text{back}} > 21$"
- [ ] 全文已无 "v1.0 / v1.1 / 补丁新增"、"四 + 1"、"向后兼容"等版本演化措辞
- [ ] §2 假设 16、17 已新增
- [ ] §3.2 已新增 $\tau^{\text{dep}}_k, \tau^{\text{back,max}}_k, T^{\text{scenic,max}}_k, \tau_{k,i}^{\text{visit\_st}}$ 四个符号
- [ ] §6.10 已删除"外部分流：节假日交通管控"
- [ ] 公式 (20)、(21)、(22)、(23)、(25)、(33)、(40)、(41)、(42)、(46)、(47) 已按本补丁修订
- [ ] §5.6 与 §5.7 已合并为 §5.6"5 套偏好方案"

---

## 8. 全文必须删除 / 替换的措辞清单

按 Ctrl-F 全文搜索逐条排查：

| 旧措辞 | 操作 | 出现位置（不完全） |
|---|---|---|
| "每日统一 8:30 出发" / "固定每日 8:30 出发" / "8:30 离开酒店" | **替换**为 day-specific 措辞 | 摘要、假设 1、假设 2、§5.3、§6.4、§6.8 |
| "$T_k^{\text{all}} \le 14$" | **删除或改为** $T_k^{\text{scenic}} \le T^{\text{scenic,max}}_k$ | 摘要、假设 5、(C7)、§5.8、§5.9 |
| "活动窗 14 h" | **替换**为"day-specific 上限" | 同上 |
| "v1.1 新增 robust" / "v1.0 四套 + v1.1 第 5 套" / "向后兼容" | **删除"v1.1 新增"等版本演化标签** | 摘要、§3.2 符号表、§5.6 标题、§5.7 标题与正文、§6.1、§6.8 标题、§7.1 (7) |
| "$T_k^{\text{scenic}} > 12.5 \vee \tau_k^{\text{back}} > 21$" | **整体改为** $\tau_k^{\text{back}} > \tau^{\text{back,max}}_k$ | 摘要、假设 13、(47) |
| "12.5"（作为 $T^{\text{scenic,max}}$ 的统一值） | **改为** day-specific 数组 $[9.5, 12.5, 12.5, 12.5, 8.0]$ | 摘要、假设 5、(33) |
| "四 / 五套方案" / "4 + 1 套" | **统一为** "5 套" | 摘要、§1.3 (5)、§5.6 标题、§5.7、§7.1 |
| "外部分流：节假日交通管控" / "通过节假日交通管控将 $p^{\text{pk}}$ 降至 …" | **整段删除**或改为"扰动假设减弱（敏感性分析）" | §6.10 (c)(i)、摘要主要结论 (4) |
| "为回应 $R \ge 90\%$ 目标" | **改为**"在 $R \ge 90\%$ 假设性达标要求下识别薄弱点" | 摘要、§1.3、§5.7、§6.8 |
| "导致失败的贡献率" | **建议改为**"在失败日中出现的频率"（避免因果性误读） | §6.11 |
| "$F_{\text{robust}}$" 符号 | **改为** $F$（统一符号） | §3.2 符号表、(35) |

---

## 9. 晚上填论文前的最终检查清单

按以下顺序逐项核对（与 §D.7 配合使用）：

### 9.1 结构层（10 分钟）

- [ ] 摘要全文搜索"待填"，命中数为 0（在数值跑完后）
- [ ] 摘要、§2、§3、§5、§6 已无版本演化措辞（v1.0、v1.1、补丁新增）
- [ ] §5.6 与 §5.7 已合并；目录与页码同步更新
- [ ] 公式编号 (1)–(54) 连续；若有合并造成的空号，已用"（已合并入 ...）"注明

### 9.2 边界一致性层（15 分钟）

- [ ] 假设 16、17 已新增到 §2，编号正确
- [ ] §3.2 符号表已新增 $\tau^{\text{dep}}_k, \tau^{\text{back,max}}_k, T^{\text{scenic,max}}_k, \tau_{k,i}^{\text{visit\_st}}$
- [ ] §3.2 $T_k^{\text{scenic}}$ 定义已改为 $\tau_k^{\text{back}} - \tau^{\text{dep}}_k$
- [ ] §3.2 $T_k^{\text{all}}$ 行已删除（或注明为诊断量不参与校验）
- [ ] §4.3 公式 (4) 已加问题一专有基准的脚注
- [ ] (C7) / (C8) / (C9) 已按 §4.1 表格修订
- [ ] 公式 (23) / (25) / (40) / (41) / (42) / (44) / (46) 已按本补丁修订
- [ ] 公式 (33) 中 $B_k$ 上限已改为 $T^{\text{scenic,max}}_k$
- [ ] 公式 (47) 已改为两项 OR

### 9.3 措辞净化层（15 分钟）

- [ ] Ctrl-F "8:30"：仅允许在问题一脚注、Day 2-4 实例处出现
- [ ] Ctrl-F "14"：仅允许在固定边界耗时合计（4+4+0.5+0.5+1.5+...）中出现，不应出现"活动窗 14"
- [ ] Ctrl-F "12.5"：仅允许作为 Day 2-4 的 $T^{\text{scenic,max}}$ 实例出现，不应作为"统一上限"
- [ ] Ctrl-F "v1.0" / "v1.1" / "补丁"：全文应无
- [ ] Ctrl-F "外部分流" / "节假日交通管控"：全文应无
- [ ] Ctrl-F "四套" / "五套"：仅出现于 "5 套" 表述（不应有"四套"或"四 / 五套"）
- [ ] Ctrl-F "$F_{\text{robust}}$"：全文应无（统一为 $F$）

### 9.4 数值层（30 分钟，需 Codex 跑完后）

- [ ] 摘要数值 = §5.9 / §6.11 数值 = 表 6 / 表 7 / 表 8 / 表 9 / 表 10 数值
- [ ] `baseline_plan.json` 顶层有 `boundary_v111` 字段
- [ ] `reliability_report.json` 的 `assumptions.boundary_v111_applied = true`
- [ ] `baseline_plan.json` 每个 day 含 `depart, T_scenic_max, back_max, buffer, visit_st`
- [ ] Day 1 depart = 11.5；Day 5 back ≤ 16.5
- [ ] 三方案对比表中 $Z_4$ 计算基于 day-specific 上限（即 $\min_k (T^{\text{scenic,max}}_k - T_k^{\text{scenic}})$，而非 $\min_k (12.5 - T_k^{\text{scenic}})$）

### 9.5 论文表述风险复核层（10 分钟）

- [ ] §2 假设 16 末尾已显式声明 Day 1 "7:00 家出发" 与 Day 5 "21:00 到家" 为建模假设
- [ ] §5.4 末尾已陈述 $Z_3$ vs $Z_4$ 的"天间齐平 vs 逐日宽裕"两层均衡含义
- [ ] §5.6 末尾已解释"为什么 5 套偏好中只有 robust 使用 $Z_4$"
- [ ] §6.10 标题与措辞已统一为"假设性达标要求"，不存在"达标 / 未达标"二元表述
- [ ] 摘要、§4.x 已说明堵车概率 $p^{\text{pk}}, p^{\text{off}}$ 为建模假设（题目未给定）
- [ ] §6.11 末尾已新增 Day 1 / Day 5 窄窗结构性影响段

### 9.6 格式层（最后 5 分钟）

- [ ] 表 1–10、图 1–6 全部在正文中引用
- [ ] 摘要 800–1000 字
- [ ] 正文（含图表）≤ 30 页
- [ ] AI 工具使用情况已声明（使用了则附 PDF；未使用则在参考文献后声明）
- [ ] 附录 D 待填项清单 0 命中

---

**补丁文档结束。**

本补丁仅做"定位 + 操作"，不重写论文、不填数值、不引入新指标。所有修改均可追溯至 `model_spec_v1.1.1_boundary_patch.md` 中明确章节。
