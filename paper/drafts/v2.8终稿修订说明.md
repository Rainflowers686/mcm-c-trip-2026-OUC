# v2.8 终稿修订说明

本轮只处理 v2.7 审查报告中剩余的排版与渲染问题，没有修改模型、参数、核心结果数值和 target90 定位。

## 已处理

1. 重新导出图 7.1：取消图内英文标题，图例移入图内左上方，避免标题与图例重叠；`2.10` 标签增加白底并上移，避免与 target R=0.9 红线视觉冲突。
2. 表 6.1、表 6.2、表 7.1 改为不可拆分的 LaTeX 表格，避免跨页孤行。
3. 表 6.3 调整列宽与表头对齐，改善 `Z1/Z2/Z3/Z4/Z4,mean/F` 视觉错位。
4. “参考文献”标题改为居中黑体四号样式，与一级标题观感一致。
5. 表 9.1 的 low_commute 可靠度列改为“参考robust”，不再用容易误读为实测值的约等数值。

## 说明

- 没有把 low_commute 的可靠度改成严格等于 robust，因为 v2.7 表 6.3 显示 low_commute 与 robust 的 $Z_4$ 不同，说明其具体时序可能并不完全相同；在未独立模拟的情况下，用“参考 robust”比给精确等号更稳妥。
- target90 定位、AI 工具标注、参考文献编号、队员信息均未改动。
