# v2.11 终稿排版修复说明

本版在 v2.10 基础上只修复提交前硬伤，不改模型、不改参数、不改结果数值。

## 已修复 A 级问题

1. **封面队员信息表**
   - 邮箱列改为分行显示，避免邮箱被截断。

2. **图 7.1 标题渲染**
   - 将 Markdown 自动图题改为 LaTeX 手动图题；
   - 删除“图 1:”自动编号叠加；
   - 将异常的 `Pextfail` 修为 `P_fail` 数学标注。

3. **附录 D 表 D.1 / D.2 路径溢出**
   - 将长路径列改为 `\path{}` 自动断行格式；
   - 使用小字号和固定列宽，确保 `.png`、`.json`、`sensitivity_grid`、`per_day_success` 等后缀完整显示。

4. **附录 A 表 A.1 长文件名问题**
   - 长脚本名与 v2.9 输出文件统一改为可断行路径；
   - 将 `.json/csv/md` 改为“含 json/csv/md 三份”的说明，提升可读性。

5. **正文引用完整性**
   - 在 §1.1 题目数据来源处补 `[5]`；
   - 在 §6.3 多目标加权和处补 `[1]`；
   - 参考文献 [1]–[8] 改为逐条独立显示。

6. **附录 C 参数表路径溢出**
   - 将 v2.9 两条长 JSON 路径改为可断行路径，避免超出右边界。

## 已修复 B 级问题

1. 重新导出图 8.1，移动图例并提高 y 轴上限，避免图例、红线与柱顶数值重叠。
2. 重新导出图 9.1，移动 R=0.90 标注，避免 target90 柱顶数值与图例重叠。
3. 对附录设定类表格补充说明：表 6.1、表 6.2、表 7.1、表 8.1–8.3 为模型设定或搜索空间说明表，不单列外部数据源。

## 自检结果

- 旧口径关键词：`待填 / 待补 / 题目题设 / 题给参数 / 高峰尾段 / 每日统一 8:30 / target90 是问题二 / 通过调参` 均为 0。
- 正文已存在 [1] 与 [5] 引用点。
- 图 7.1 不再出现“图 1:”自动编号叠加或 `Pextfail` 异常渲染。
- 附录 A、C、D 的长路径表格已重新渲染检查，无明显右边界溢出或列重叠。
